# Phase I Public Documentation

This directory is the Phase I public documentation corpus for the Semantic Computational Runtime.

It is intentionally a coherent explanatory layer rather than a duplicate of the root README, internal specifications, or implementation.

See `000_index.md` for the reader entrypoint and `_meta/` for documentation governance.
