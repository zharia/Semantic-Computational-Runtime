# Phase 0 executable seed.
#
# This file intentionally contains no messaging implementation.
# It proves that the repository can compile and execute a native Mojo program.

def main() raises:
    print("Hyrx Phase 0 seed")
    print("status=bootstrap")
