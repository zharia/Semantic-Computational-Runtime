# AMQP 0-9-1

HyrxMQ targets AMQP 0-9-1 for external interoperability.

AMQP 0-9-1 and AMQP 1.0 are different protocols. Do not assume compatibility with AMQP 1.0 from AMQP 0-9-1 support.

Consult the project compatibility matrix for the currently supported features and RabbitMQ extensions.
