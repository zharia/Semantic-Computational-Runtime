# Technical References

Technical references explain external implementation systems used by SCR. They are subordinate to the normative semantic specifications.

The existing MLIR references belong here. In particular, MLIR should be treated as the principal IR/compiler infrastructure used to manifest and transform SCR semantics, not as the definition of the Semantic Field.
