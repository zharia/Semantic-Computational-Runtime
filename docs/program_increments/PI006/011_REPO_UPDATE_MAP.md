# Repository Update Map

Review/update root README and architecture/milestone documents so they no longer present
STC as the fundamental ontology.

SCRFormal:
- preserve canonical Algebra;
- separate reference-specialization laws from universal semantics;
- add derived transition semantics where needed;
- formalize domain laws;
- avoid duplicate ontology.

lib:
- use established conventions;
- initial targets: core, mathematics, data, graph.

scripts:
- validate domain metadata, graph consistency, dependency cycles, specification completeness,
  status validity and semantic/implementation mappings.

program_increments/v0.0.1:
- add PI006 artifacts;
- retain historical records but explicitly supersede obsolete claims.
